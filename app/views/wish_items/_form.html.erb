<%= form_for([@wish_item.wish_list, @wish_item]) do |f| %>
  <% if @wish_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wish_item.errors.count, "error") %> prohibited this wish_item from being saved:</h2>

      <ul>
      <% @wish_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :completed %><br />
    <%= f.check_box :completed %>
  </div>
  <div class="field">
    <%= f.label :completed_at %><br />
    <%= f.datetime_field :completed_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
